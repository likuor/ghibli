import { FC } from 'react';
import MainLayout from './Layout/MainLayout';
import './App.css';

const App: FC = (props) => {
  return (
    <div className='App'>
      <MainLayout />
    </div>
  );
};

export default App;
